

doctype html
html(lang="en")
    head
        include partials/header

    body.login
        .contenedor-formulario
            h1 Crea tu cuenta en Up-Task

            form#formulario.caja.caja-login(method="POST" action="/crear-cuenta" )
                .campo
                    label(for="email") E-mail:
                    input(type="email" name="email" placeholder="Escribe tu email" required)
                .campo
                    label(for="password") Password:
                    input(type="password" name="password" placeholder="Escribe tu contraseña" required)
                .campo.enviar
                    input.boton(type="submit" value="Crear Cuenta")
                .campo
                    a(href="/iniciar-sesion") << Volver a iniciar sección

        script(src="/dist/bundle.js")
