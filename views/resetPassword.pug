doctype html
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1= nombrePagina
            p Coloca tu nuevo password


            form.caja-login(method="POST")
                .campo
                    label(for="password") Password:
                    input(type="password" name="password" placeholder="Escribe tu nuevo password")
                .campo.enviar
                    input.boton(type="submit" value="Reestablecer password")
                .campo.acciones
                    a(href="/iniciar-sesion") << Volver a login!
